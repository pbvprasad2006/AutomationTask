name: Cypress Tests

on:
  push:
    branches:
      - main

jobs:
  cypress_tests:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Run Cypress Tests
        run: cypress-io/github-action@v5